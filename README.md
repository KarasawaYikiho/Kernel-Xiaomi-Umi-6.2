# Kernel-Xiaomi-Umi

English | [简体中文](README-Zh-CN.md)

Kernel porting orchestrator for Xiaomi 10 (`umi`). CI-driven migration from SO-TS 4.19 to 5+ baseline.

> This repo is **not** a kernel source tree.

## Scope

- Phase2 migration automation via GitHub Actions
- Structured diagnostics and metrics
- Flash-ready artifact delivery

## Quick Start

Run **`ROM-Aligned-Umi-Port.yml`** workflow, then inspect:

1. `artifacts/phase2-report.txt`
2. `artifacts/next-focus.txt`
3. `artifacts/runtime-validation-summary.md`
4. `artifacts/anykernel-info.txt`

## Inputs

| Input | Default | Description |
|-------|---------|-------------|
| `device` | `umi` | Device codename |
| `source_repo` | SO-TS 4.19 repo | Source kernel |
| `target_repo` | yefxx 5+ repo | Target kernel |
| `bootimg_required_bytes` | `134217728` | Target boot.img size |
| `bootimg_ramdisk_url` | (optional) | Custom ramdisk |
| `bootimg_prebuilt_url` | (optional) | Fallback boot.img URL, preferred over committing stock boot.img |
| `official_rom_zip` | (optional) | Official ROM zip path or URL for alignment baseline |

## Workflows

- **`ROM-Aligned-Umi-Port.yml`** — Main ROM-aware migration workflow
- **`Build-Umi-Kernel.yml`** — Reference cloud build

## Repository Hygiene

- `artifacts/`, `source/`, and `target/` are local working directories generated by workflows and validation runs
- `.ruff_cache/` and `__pycache__/` are local tool caches
- Keep those paths untracked so the repo stays reviewable and clean
- `Porting/OfficialRomBaseline/` stores ROM baseline metadata plus compact validation images intended for repo-side checks
- See `Porting/README.md` for document order and `Tools/Porting/README.md` for validation entry points

## Reference Sources

- SO-TS: `android_kernel_xiaomi_sm8250` (4.19)
- 5+ baseline: `yefxx/xiaomi-umi-linux-kernel`
- Driver refs: `UtsavBalar1231/*`

## License

GPL-2.0-only
