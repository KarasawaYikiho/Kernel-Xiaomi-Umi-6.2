# Kernel-Xiaomi-SM8250

English | [简体中文](README-Zh-Cn.md)

Xiaomi SM8250-family kernel source tree based on `yefxx/xiaomi-umi-linux-kernel`, with `umi` as the current ROM-alignment reference device.

This repo is now a kernel source tree. SO-TS 4.19 is reference-only for targeted comparisons, and the official Xiaomi ROM is validation-only evidence for boot-chain and packaging alignment.

## Branch Policy

- `main`: lightweight orchestration branch without imported Linux kernel source.
- `master`: full yefxx-based kernel-source branch; kernel GitHub Actions run here.
- Persistent agent/project rules are in `AGENTS.md`.

## Scope

- Linux kernel source, DTS, defconfig, drivers, CI, scripts, and docs
- Phase2 migration and ROM-alignment automation via GitHub Actions
- Structured diagnostics, metrics, and flash-ready artifact delivery

## Quick Start

Run **`Build-Umi-Kernel.yml`** to build the checked-out kernel tree, or **`ROM-Aligned-Umi-Port.yml`** for ROM-aligned evidence and packaging. Inspect:

1. `artifacts/phase2-report.txt`
2. `artifacts/next-focus.txt`
3. `artifacts/runtime-validation-summary.md`
4. `artifacts/anykernel-info.txt`

## Inputs

| Input | Default | Description |
|-------|---------|-------------|
| `device` | `umi` | Device codename; current default baseline remains `umi` |

Reference locations, default branches, toolchain source, and boot image baseline paths are no longer workflow inputs. The workflow auto-fills them in code:

- yefxx baseline metadata, SO-TS reference metadata, toolchain download source, and ROM defaults are centralized in `Porting/Sm8250PortConfig.json`
- Local ROM dir for maintainer runs: `D:\GIT\MIUI_UMI`
- Local ROM zip fallback for maintainer runs: `D:\GIT\MIUI_UMI_OS1.0.5.0.TJBCNXM_d01651ed86_13.0.zip`
- GitHub Action fallback: tracked `Porting/OfficialRomBaseline/BootImgParts/` chunks auto-reassembled in CI
- `BOOTIMG_REQUIRED_BYTES`: derived from the active ROM baseline first, then falls back only when baseline metadata is unavailable

## Workflows

- **`ROM-Aligned-Umi-Port.yml`** — ROM-aware migration workflow for the current SM8250 baseline on `master`
- **`Build-Umi-Kernel.yml`** — Cloud kernel build for the current SM8250 baseline on `master`

## Local Boot Baseline

For the shortest Windows path on this machine, run:

Use `Porting/Tools/RunLocalOfficialRomBaseline.ps1`; refreshed outputs and local validation details stay centralized in `Porting/Tools/README.md` to avoid duplicated maintenance.

It defaults to `D:\GIT\MIUI_UMI` and keeps the official `boot.img` out of git.

## Repository Hygiene

- `artifacts/`, `out/`, `source/`, and `target/` are local working directories generated by workflows and validation runs
- `source.extract/`, `target.extract/`, and `source.zip` are local fetch/extract outputs and should also stay untracked
- `.ruff_cache/` and `__pycache__/` are local tool caches
- Keep those paths untracked so the repo stays reviewable and clean
- `Porting/OfficialRomBaseline/` stores ROM baseline metadata plus compact validation images intended for repo-side checks
- See `PortingPlan.md` for the execution baseline, `Porting/README.md` for document order, and `Porting/Tools/README.md` for validation entry points

## Reference Sources

- Source baseline: `yefxx/xiaomi-umi-linux-kernel` (`master`, Linux 6.11 snapshot)
- Reference-only SO-TS: `android_kernel_xiaomi_sm8250` (4.19)
- Driver refs: `UtsavBalar1231/*`
- Unified config source: `Porting/Sm8250PortConfig.json`

## License

GPL-2.0-only
